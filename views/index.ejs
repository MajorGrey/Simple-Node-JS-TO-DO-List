<?- include('inc/header')?>
    <section>
        <div class="container">
            <h1 class="font-weight-bolder"> simple to do list</h1>

        </div>
    </section>
    <section class="pt-5">
        <div class="container">
            <div class="mx-auto ">
                <div class="d-flex justify-content-center pb-5">
                    <form class="form-inline" method="post">
                        <div class="form-group mb-2">
                            <label for="inputPassword2" class="sr-only rounded-0">Password</label>
                            <input type="text" name="title" class="small-box form-control form-control-lg rounded-0 shadow-none" placeholder="Title">
                            <input type="text" name="item" class="big-box form-control form-control-lg rounded-0 shadow-none" placeholder="Add new item">
                        </div>
                        <button class="btn btn-info btn-lg mb-2 rounded-0 shadow-none">Add Item</button>
                    </form>
                </div>

                <div class="row" id="datas">
                    <? data.forEach( (item)=>{?>
                        <div class="col-12 col-sm-6 col-md-6 col-lg-4 col-xl-4">
                            <div class="card" style="width: 18rem;">
                                <div class="card-body">
                                    <h5 class="text-dark pb-2">
                                        <?= item.title?>
                                    </h5>
                                    <p class="card-subtitle mb-2 text-muted small">
                                        <?= moment(item.date).fromNow()?>
                                    </p>
                                    <p class="card-text text-muted">
                                        <?=item.item ?>
                                    </p>
                                    <a href="#" class="card-link btn btn-light">Update</a>
                                    <a id="del" data-id="<?= item.id?>" href="#" class="float-right btn btn-light">Delete</a>
                                </div>
                            </div>
                        </div>
                        <? });?>
                </div>
            </div>
        </div>
    </section>
    <?- include('inc/footer')?>